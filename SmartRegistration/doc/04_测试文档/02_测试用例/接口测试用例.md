# 接口测试用例

## 1. 概述

本文档是智能挂号预约系统的接口测试用例，详细说明了各API接口的测试场景、请求参数和预期响应。

## 2. 认证模块接口测试

### 2.1 登录接口

| 用例ID | 接口 | 方法 | 测试场景 | 请求体 | 预期响应 |
|--------|------|------|----------|--------|----------|
| API-AUTH-001 | /auth/login | POST | 正确登录 | `{"username":"admin","password":"password"}` | 200 OK，返回token和用户信息 |
| API-AUTH-002 | /auth/login | POST | 错误密码 | `{"username":"admin","password":"wrong"}` | 401 Unauthorized，返回错误信息 |
| API-AUTH-003 | /auth/login | POST | 不存在用户 | `{"username":"nonexistent","password":"password"}` | 401 Unauthorized，返回错误信息 |
| API-AUTH-004 | /auth/login | POST | 空用户名 | `{"username":"","password":"password"}` | 400 Bad Request，返回错误信息 |
| API-AUTH-005 | /auth/login | POST | 空密码 | `{"username":"admin","password":""}` | 400 Bad Request，返回错误信息 |

### 2.2 注册接口

| 用例ID | 接口 | 方法 | 测试场景 | 请求体 | 预期响应 |
|--------|------|------|----------|--------|----------|
| API-AUTH-006 | /auth/register | POST | 完整注册 | `{"username":"newuser","password":"password","name":"新用户","gender":"男","age":25,"phone":"13800138000","email":"newuser@example.com","role":"PATIENT","status":"ACTIVE"}` | 201 Created，返回用户信息 |
| API-AUTH-007 | /auth/register | POST | 用户名已存在 | `{"username":"admin","password":"password","name":"管理员","gender":"男","age":30,"phone":"13800138000","email":"admin@example.com","role":"ADMIN","status":"ACTIVE"}` | 400 Bad Request，返回错误信息 |
| API-AUTH-008 | /auth/register | POST | 缺少必填字段 | `{"username":"newuser","password":"password"}` | 400 Bad Request，返回错误信息 |

### 2.3 刷新令牌接口

| 用例ID | 接口 | 方法 | 测试场景 | 请求体 | 预期响应 |
|--------|------|------|----------|--------|----------|
| API-AUTH-009 | /auth/refresh | POST | 有效令牌 | `{"token":"valid_token"}` | 200 OK，返回新token |
| API-AUTH-010 | /auth/refresh | POST | 无效令牌 | `{"token":"invalid_token"}` | 401 Unauthorized，返回错误信息 |

## 3. 用户模块接口测试

### 3.1 用户管理接口

| 用例ID | 接口 | 方法 | 测试场景 | 请求体 | 预期响应 |
|--------|------|------|----------|--------|----------|
| API-USER-001 | /users | GET | 获取用户列表 | N/A | 200 OK，返回用户列表 |
| API-USER-002 | /users/{id} | GET | 获取用户详情 | N/A | 200 OK，返回用户详情 |
| API-USER-003 | /users | POST | 创建用户 | `{"username":"user1","password":"password","name":"用户1","gender":"男","age":30,"phone":"13800138000","email":"user1@example.com","role":"PATIENT","status":"ACTIVE"}` | 201 Created，返回用户信息 |
| API-USER-004 | /users/{id} | PUT | 更新用户 | `{"name":"用户1更新","phone":"13900139000"}` | 200 OK，返回更新后的用户信息 |
| API-USER-005 | /users/{id} | DELETE | 删除用户 | N/A | 200 OK，返回成功信息 |

### 3.2 个人信息接口

| 用例ID | 接口 | 方法 | 测试场景 | 请求体 | 预期响应 |
|--------|------|------|----------|--------|----------|
| API-USER-006 | /users/me | GET | 获取个人信息 | N/A | 200 OK，返回当前用户信息 |
| API-USER-007 | /users/me | PUT | 更新个人信息 | `{"name":"我的名字","phone":"13800138000"}` | 200 OK，返回更新后的信息 |
| API-USER-008 | /users/me/password | PUT | 修改密码 | `{"oldPassword":"password","newPassword":"newpassword123"}` | 200 OK，返回成功信息 |

## 4. 科室模块接口测试

### 4.1 科室管理接口

| 用例ID | 接口 | 方法 | 测试场景 | 请求体 | 预期响应 |
|--------|------|------|----------|--------|----------|
| API-DEPT-001 | /departments | GET | 获取科室列表 | N/A | 200 OK，返回科室列表 |
| API-DEPT-002 | /departments/{id} | GET | 获取科室详情 | N/A | 200 OK，返回科室详情 |
| API-DEPT-003 | /departments | POST | 创建科室 | `{"name":"新科室","description":"新科室描述","status":"ACTIVE"}` | 201 Created，返回科室信息 |
| API-DEPT-004 | /departments/{id} | PUT | 更新科室 | `{"name":"科室更新","description":"更新后的描述"}` | 200 OK，返回更新后的科室信息 |
| API-DEPT-005 | /departments/{id} | DELETE | 删除科室 | N/A | 200 OK，返回成功信息 |

## 5. 医生模块接口测试

### 5.1 医生管理接口

| 用例ID | 接口 | 方法 | 测试场景 | 请求体 | 预期响应 |
|--------|------|------|----------|--------|----------|
| API-DOCTOR-001 | /doctors | GET | 获取医生列表 | N/A | 200 OK，返回医生列表 |
| API-DOCTOR-002 | /doctors | GET | 按科室查询 | N/A（参数：departmentId=1） | 200 OK，返回指定科室的医生列表 |
| API-DOCTOR-003 | /doctors/{id} | GET | 获取医生详情 | N/A | 200 OK，返回医生详情 |
| API-DOCTOR-004 | /doctors | POST | 创建医生 | `{"userId":2,"departmentId":1,"title":"主治医师","specialty":"心血管疾病","bio":"医生简介","status":"ACTIVE"}` | 201 Created，返回医生信息 |
| API-DOCTOR-005 | /doctors/{id} | PUT | 更新医生 | `{"title":"副主任医师","specialty":"心血管疾病、高血压"}` | 200 OK，返回更新后的医生信息 |
| API-DOCTOR-006 | /doctors/{id} | DELETE | 删除医生 | N/A | 200 OK，返回成功信息 |
| API-DOCTOR-007 | /doctors/{id}/schedules | GET | 获取医生排班 | N/A | 200 OK，返回医生排班列表 |

## 6. 排班模块接口测试

### 6.1 排班管理接口

| 用例ID | 接口 | 方法 | 测试场景 | 请求体 | 预期响应 |
|--------|------|------|----------|--------|----------|
| API-SCHEDULE-001 | /schedules | GET | 获取排班列表 | N/A | 200 OK，返回排班列表 |
| API-SCHEDULE-002 | /schedules | GET | 按医生查询 | N/A（参数：doctorId=1） | 200 OK，返回指定医生的排班列表 |
| API-SCHEDULE-003 | /schedules | GET | 按科室查询 | N/A（参数：departmentId=1） | 200 OK，返回指定科室的排班列表 |
| API-SCHEDULE-004 | /schedules/{id} | GET | 获取排班详情 | N/A | 200 OK，返回排班详情 |
| API-SCHEDULE-005 | /schedules | POST | 创建排班 | `{"doctorId":1,"departmentId":1,"date":"2026-05-01","startTime":"08:00","endTime":"12:00","totalSlots":20,"availableSlots":20,"status":"ACTIVE"}` | 201 Created，返回排班信息 |
| API-SCHEDULE-006 | /schedules/{id} | PUT | 更新排班 | `{"totalSlots":25,"availableSlots":25}` | 200 OK，返回更新后的排班信息 |
| API-SCHEDULE-007 | /schedules/{id} | DELETE | 删除排班 | N/A | 200 OK，返回成功信息 |
| API-SCHEDULE-008 | /schedules/department/{departmentId} | GET | 获取科室排班 | N/A | 200 OK，返回科室排班列表 |
| API-SCHEDULE-009 | /schedules/doctor/{doctorId} | GET | 获取医生排班 | N/A | 200 OK，返回医生排班列表 |
| API-SCHEDULE-010 | /schedules/doctor/me | GET | 获取我的排班 | N/A | 200 OK，返回当前医生的排班列表 |

## 7. 预约模块接口测试

### 7.1 预约管理接口

| 用例ID | 接口 | 方法 | 测试场景 | 请求体 | 预期响应 |
|--------|------|------|----------|--------|----------|
| API-APPOINTMENT-001 | /appointments | GET | 获取预约列表 | N/A | 200 OK，返回预约列表 |
| API-APPOINTMENT-002 | /appointments | GET | 按状态查询 | N/A（参数：status=PENDING） | 200 OK，返回指定状态的预约列表 |
| API-APPOINTMENT-003 | /appointments/{id} | GET | 获取预约详情 | N/A | 200 OK，返回预约详情 |
| API-APPOINTMENT-004 | /appointments | POST | 创建预约 | `{"patientId":3,"doctorId":1,"scheduleId":1,"symptoms":"胸痛、胸闷","notes":"无"}` | 201 Created，返回预约信息 |
| API-APPOINTMENT-005 | /appointments/{id} | PUT | 更新预约 | `{"status":"CONFIRMED"}` | 200 OK，返回更新后的预约信息 |
| API-APPOINTMENT-006 | /appointments/{id}/cancel | PUT | 取消预约 | N/A | 200 OK，返回成功信息 |
| API-APPOINTMENT-007 | /appointments/{id}/complete | PUT | 完成预约 | N/A | 200 OK，返回成功信息 |
| API-APPOINTMENT-008 | /appointments/patient/{patientId} | GET | 获取患者预约 | N/A | 200 OK，返回患者的预约列表 |
| API-APPOINTMENT-009 | /appointments/patient/me | GET | 获取我的预约 | N/A | 200 OK，返回当前患者的预约列表 |
| API-APPOINTMENT-010 | /appointments/doctor/{doctorId} | GET | 获取医生预约 | N/A | 200 OK，返回医生的预约列表 |
| API-APPOINTMENT-011 | /appointments/doctor/me | GET | 获取我的医生预约 | N/A | 200 OK，返回当前医生的预约列表 |

## 8. 系统模块接口测试

### 8.1 健康检查接口

| 用例ID | 接口 | 方法 | 测试场景 | 请求体 | 预期响应 |
|--------|------|------|----------|--------|----------|
| API-SYSTEM-001 | /health | GET | 健康检查 | N/A | 200 OK，返回系统健康状态 |
| API-SYSTEM-002 | /health/info | GET | 系统信息 | N/A | 200 OK，返回系统信息 |
| API-SYSTEM-003 | /health/ping | GET | 心跳检查 | N/A | 200 OK，返回pong |

## 9. 认证豁免模块接口测试

### 9.1 认证豁免接口

| 用例ID | 接口 | 方法 | 测试场景 | 请求体 | 预期响应 |
|--------|------|------|----------|--------|----------|
| API-EXEMPTION-001 | /admin/auth-exemption/status | GET | 获取豁免状态 | N/A | 200 OK，返回豁免状态 |
| API-EXEMPTION-002 | /admin/auth-exemption/rules | GET | 获取豁免规则 | N/A | 200 OK，返回豁免规则列表 |
| API-EXEMPTION-003 | /admin/auth-exemption/rules | POST | 添加豁免规则 | `{"path":"/public/info","httpMethods":["GET"],"description":"公共信息接口","patternType":"ANT","enabled":true}` | 201 Created，返回添加的规则 |
| API-EXEMPTION-004 | /admin/auth-exemption/rules | DELETE | 删除豁免规则 | `{"path":"/public/info","httpMethods":["GET"]}` | 200 OK，返回成功信息 |
| API-EXEMPTION-005 | /admin/auth-exemption/reload | POST | 重新加载配置 | N/A | 200 OK，返回成功信息 |
| API-EXEMPTION-006 | /admin/auth-exemption/validate | GET | 验证配置 | N/A | 200 OK，返回配置验证结果 |
| API-EXEMPTION-007 | /admin/auth-exemption/test | POST | 测试路径豁免 | `{"path":"/auth/login","httpMethod":"POST"}` | 200 OK，返回测试结果 |

## 10. 测试执行说明

### 10.1 测试环境

- **API地址**：http://localhost:8080/api/v1
- **测试工具**：Postman
- **认证方式**：Bearer Token

### 10.2 测试顺序

1. 认证模块接口测试
2. 用户模块接口测试
3. 科室模块接口测试
4. 医生模块接口测试
5. 排班模块接口测试
6. 预约模块接口测试
7. 系统模块接口测试
8. 认证豁免模块接口测试

### 10.3 测试注意事项

- 执行测试前确保系统正常运行
- 执行需要认证的接口时，先获取token
- 记录测试过程中的错误和异常
- 按照测试用例顺序执行测试

## 11. 接口响应格式

### 11.1 成功响应

```json
{
  "code": 200,
  "message": "成功",
  "data": {
    // 响应数据
  },
  "error": null
}
```

### 11.2 错误响应

```json
{
  "code": 400,
  "message": "请求错误",
  "data": null,
  "error": "错误信息"
}
```

## 12. 总结

本接口测试用例文档详细说明了智能挂号预约系统各API接口的测试场景、请求参数和预期响应。通过执行这些测试用例，可以验证系统的API接口是否正常工作，确保接口符合设计规范。测试团队应严格按照本测试用例执行测试，确保测试的全面性和有效性。

---

*文档版本：1.0.0*
*最后更新：2026-04-23*
