# 智能挂号预约系统 数据库设计

## 1. 数据库概述

智能挂号预约系统使用 MySQL 8.0+ 作为数据库，采用关系型数据库设计，通过表之间的关联关系实现业务逻辑。数据库设计遵循第三范式，确保数据的一致性和完整性。

## 2. 核心表结构

### 2.1 用户表 (users)

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|---------|------|------|
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 用户ID |
| username | VARCHAR(50) | UNIQUE, NOT NULL | 用户名 |
| password | VARCHAR(100) | NOT NULL | 密码（哈希存储） |
| name | VARCHAR(50) | NOT NULL | 姓名 |
| gender | VARCHAR(10) | NOT NULL | 性别 |
| age | INT | NOT NULL | 年龄 |
| phone | VARCHAR(20) | UNIQUE, NOT NULL | 手机号码 |
| email | VARCHAR(100) | UNIQUE | 邮箱 |
| id_card | VARCHAR(18) | UNIQUE | 身份证号 |
| role | VARCHAR(20) | NOT NULL | 角色（ADMIN/DOCTOR/PATIENT） |
| status | VARCHAR(20) | NOT NULL | 状态（ACTIVE/INACTIVE） |
| created_at | TIMESTAMP | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | NOT NULL, DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

### 2.2 科室表 (departments)

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|---------|------|------|
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 科室ID |
| name | VARCHAR(50) | UNIQUE, NOT NULL | 科室名称 |
| description | VARCHAR(255) | | 科室描述 |
| status | VARCHAR(20) | NOT NULL | 状态（ACTIVE/INACTIVE） |
| created_at | TIMESTAMP | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | NOT NULL, DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

### 2.3 医生表 (doctors)

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|---------|------|------|
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 医生ID |
| user_id | BIGINT | FOREIGN KEY (users.id), UNIQUE, NOT NULL | 用户ID（关联用户表） |
| department_id | BIGINT | FOREIGN KEY (departments.id), NOT NULL | 科室ID（关联科室表） |
| title | VARCHAR(50) | NOT NULL | 职称 |
| specialty | VARCHAR(100) | | 专长 |
| bio | VARCHAR(255) | | 简介 |
| status | VARCHAR(20) | NOT NULL | 状态（ACTIVE/INACTIVE） |
| created_at | TIMESTAMP | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | NOT NULL, DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

### 2.4 排班表 (schedules)

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|---------|------|------|
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 排班ID |
| doctor_id | BIGINT | FOREIGN KEY (doctors.id), NOT NULL | 医生ID（关联医生表） |
| date | DATE | NOT NULL | 排班日期 |
| start_time | TIME | NOT NULL | 开始时间 |
| end_time | TIME | NOT NULL | 结束时间 |
| max_patients | INT | NOT NULL | 最大预约人数 |
| available_slots | INT | NOT NULL | 可用预约数 |
| status | VARCHAR(20) | NOT NULL | 状态（ACTIVE/CANCELLED） |
| created_at | TIMESTAMP | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | NOT NULL, DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

### 2.5 预约表 (appointments)

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|---------|------|------|
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 预约ID |
| patient_id | BIGINT | FOREIGN KEY (users.id), NOT NULL | 患者ID（关联用户表） |
| doctor_id | BIGINT | FOREIGN KEY (doctors.id), NOT NULL | 医生ID（关联医生表） |
| schedule_id | BIGINT | FOREIGN KEY (schedules.id), NOT NULL | 排班ID（关联排班表） |
| appointment_time | DATETIME | NOT NULL | 预约时间 |
| status | VARCHAR(20) | NOT NULL | 状态（PENDING/CONFIRMED/COMPLETED/CANCELLED） |
| symptoms | VARCHAR(255) | | 症状描述 |
| created_at | TIMESTAMP | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | NOT NULL, DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

## 3. 表关系图

```mermaid
erDiagram
    USERS ||--o{ DOCTORS : has
    USERS ||--o{ APPOINTMENTS : has
    DEPARTMENTS ||--o{ DOCTORS : contains
    DOCTORS ||--o{ SCHEDULES : has
    DOCTORS ||--o{ APPOINTMENTS : has
    SCHEDULES ||--o{ APPOINTMENTS : has

    USERS {
        BIGINT id PK
        VARCHAR(50) username UK
        VARCHAR(100) password
        VARCHAR(50) name
        VARCHAR(10) gender
        INT age
        VARCHAR(20) phone UK
        VARCHAR(100) email UK
        VARCHAR(18) id_card UK
        VARCHAR(20) role
        VARCHAR(20) status
        TIMESTAMP created_at
        TIMESTAMP updated_at
    }

    DEPARTMENTS {
        BIGINT id PK
        VARCHAR(50) name UK
        VARCHAR(255) description
        VARCHAR(20) status
        TIMESTAMP created_at
        TIMESTAMP updated_at
    }

    DOCTORS {
        BIGINT id PK
        BIGINT user_id FK UK
        BIGINT department_id FK
        VARCHAR(50) title
        VARCHAR(100) specialty
        VARCHAR(255) bio
        VARCHAR(20) status
        TIMESTAMP created_at
        TIMESTAMP updated_at
    }

    SCHEDULES {
        BIGINT id PK
        BIGINT doctor_id FK
        DATE date
        TIME start_time
        TIME end_time
        INT max_patients
        INT available_slots
        VARCHAR(20) status
        TIMESTAMP created_at
        TIMESTAMP updated_at
    }

    APPOINTMENTS {
        BIGINT id PK
        BIGINT patient_id FK
        BIGINT doctor_id FK
        BIGINT schedule_id FK
        DATETIME appointment_time
        VARCHAR(20) status
        VARCHAR(255) symptoms
        TIMESTAMP created_at
        TIMESTAMP updated_at
    }
```

## 4. 索引策略

### 4.1 用户表 (users)

| 索引名 | 索引类型 | 字段 | 描述 |
|--------|---------|------|------|
| PRIMARY | 主键索引 | id | 唯一标识用户 |
| idx_username | 唯一索引 | username | 加速用户名查询 |
| idx_phone | 唯一索引 | phone | 加速手机号码查询 |
| idx_id_card | 唯一索引 | id_card | 加速身份证号查询 |
| idx_email | 唯一索引 | email | 加速邮箱查询 |
| idx_role | 普通索引 | role | 加速角色筛选 |
| idx_status | 普通索引 | status | 加速状态筛选 |

### 4.2 科室表 (departments)

| 索引名 | 索引类型 | 字段 | 描述 |
|--------|---------|------|------|
| PRIMARY | 主键索引 | id | 唯一标识科室 |
| idx_name | 唯一索引 | name | 加速科室名称查询 |
| idx_status | 普通索引 | status | 加速状态筛选 |

### 4.3 医生表 (doctors)

| 索引名 | 索引类型 | 字段 | 描述 |
|--------|---------|------|------|
| PRIMARY | 主键索引 | id | 唯一标识医生 |
| idx_user_id | 唯一索引 | user_id | 加速用户ID关联查询 |
| idx_department_id | 普通索引 | department_id | 加速科室筛选 |
| idx_status | 普通索引 | status | 加速状态筛选 |

### 4.4 排班表 (schedules)

| 索引名 | 索引类型 | 字段 | 描述 |
|--------|---------|------|------|
| PRIMARY | 主键索引 | id | 唯一标识排班 |
| idx_doctor_id | 普通索引 | doctor_id | 加速医生筛选 |
| idx_date | 普通索引 | date | 加速日期筛选 |
| idx_status | 普通索引 | status | 加速状态筛选 |
| idx_doctor_date | 联合索引 | (doctor_id, date) | 加速医生和日期的联合查询 |

### 4.5 预约表 (appointments)

| 索引名 | 索引类型 | 字段 | 描述 |
|--------|---------|------|------|
| PRIMARY | 主键索引 | id | 唯一标识预约 |
| idx_patient_id | 普通索引 | patient_id | 加速患者筛选 |
| idx_doctor_id | 普通索引 | doctor_id | 加速医生筛选 |
| idx_schedule_id | 普通索引 | schedule_id | 加速排班关联查询 |
| idx_status | 普通索引 | status | 加速状态筛选 |
| idx_patient_status | 联合索引 | (patient_id, status) | 加速患者和状态的联合查询 |
| idx_appointment_time | 普通索引 | appointment_time | 加速预约时间筛选 |

## 5. 数据备份与恢复方案

### 5.1 备份策略

- **每日全量备份**：
  - 时间：每天凌晨 2:00
  - 方式：使用 `mysqldump` 命令导出整个数据库
  - 存储：本地存储 + 云存储

- **每月归档备份**：
  - 时间：每月最后一天凌晨 2:00
  - 方式：使用 `mysqldump` 命令导出整个数据库并压缩
  - 存储：云存储（长期保存）

### 5.2 备份存储

- **本地存储**：
  - 路径：`/backup/mysql/`
  - 保留时间：7 天
  - 自动清理过期备份

- **云存储**：
  - 服务：AWS S3 / 阿里云 OSS
  - 保留时间：
    - 每日备份：30 天
    - 每月备份：1 年
  - 加密存储

### 5.3 恢复策略

- **恢复流程**：
  1. 停止应用服务
  2. 备份当前数据库（如果需要）
  3. 恢复备份数据
  4. 验证数据完整性
  5. 启动应用服务
  6. 测试系统功能

## 6. 数据库优化

### 6.1 性能优化

- **查询优化**：
  - 合理使用索引
  - 避免全表扫描
  - 优化复杂查询

- **配置优化**：
  - 调整 MySQL 配置参数
  - 优化内存使用
  - 调整连接池大小

- **分区表**：
  - 对大型表（如预约表）使用分区
  - 按日期或其他字段分区
  - 提高查询效率

### 6.2 安全优化

- **访问控制**：
  - 最小权限原则
  - 定期更新密码
  - 限制数据库访问 IP

- **数据加密**：
  - 敏感数据加密存储
  - 传输过程加密
  - 加密密钥管理

- **审计日志**：
  - 开启 MySQL 审计日志
  - 记录关键操作
  - 定期检查审计日志
